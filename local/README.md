# Local Overrides

Files in this directory are ignored by git except for this README and
`.gitkeep`. Use it for machine-specific shell snippets.

Common files:

- `aliases.sh`
- `exports.sh`
- `path.sh`
- `init.sh`
